// vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
  // Otras configuraciones...
  css: {
    modules: false // Cambia a true si estás utilizando CSS Modules
  }
});

